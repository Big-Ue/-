#ifndef DEF_H
#define DEF_H

// 物品類型
enum {
    efood = 1,
    eweapon
};

// 生物 / 職業類型
enum {
    efighter = 1, // 一般冒險者 (CFighter)
    emage,        // 法師 (CMage)
    eorc,         // 獸人 (COrc)
    emonster      // 怪物
};

#endif